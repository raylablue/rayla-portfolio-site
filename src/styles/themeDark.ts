import { createTheme } from '@mui/material/styles';

const themeDark = createTheme({
  palette: {
    background: {
      default: '#262526',
    },
    text: {
      primary: '#C0BDC4FF',
    },
  },
});

export default themeDark;
