import { createTheme } from '@mui/material/styles';

const themeLight = createTheme({
  palette: {
    background: {
      default: '#c0bdc4',
    },
    text: {
      primary: '#262526',
    },
  },
});

export default themeLight;
